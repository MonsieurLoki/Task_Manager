{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Application de gestionnaire de tâches responsive",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --ignore tasks.db server.js",
    "dev": "nodemon server.js",
    "reset-db": "node -e \"const fs = require('fs'); fs.unlink('tasks.db', (err) => { if (err && err.code !== 'ENOENT') { console.error(err); return; } console.log('Database reset.'); });\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "task-manager",
    "todo",
    "productivity"
  ],
  "author": "",
  "license": "ISC"
}
